<lists wx:if="{{PageCur==='home'}}"></lists>
<about wx:if="{{PageCur==='about'}}"></about>

<view class="cu-bar tabbar bg-white shadow foot">
  <view class="action" bindtap="navChange" data-cur="home">
    <view class="cuIcon-home{{PageCur==='home'?'fill':''}}"></view>
    <view class="text-gray">主页</view>
  </view>
  <view class="action" bindtap="navChange" data-cur="about">
    <view class="cuIcon-profile{{PageCur==='about'?'fill':''}}"></view>
    <view class="text-gray">关于</view>
  </view>
</view>

